# Examples

## Markdown Plan

~~~bash
launch-action-skill fixtures/sample-repo --format markdown
~~~

## JSON Plan

~~~bash
launch-action-skill fixtures/sample-repo --format json
~~~

JSON output is useful when a connector router or content agent needs structured blockers and approval gates.
